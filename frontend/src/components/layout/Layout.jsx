import PublicLayout from "./PublicLayout";

/**
 * Backwards-compatible alias so existing route wiring (`import Layout from
 * "@/components/layout/Layout"`) keeps working. The reusable shell is
 * PublicLayout.
 */
export default PublicLayout;
